# Data

`data_entries_final.json` — github.com/llm-platform-security/gpt-data-exposure projesinden proje sahibiyle iletişime geçilerek elde edilmiş veri seti. 12.811 kayıt, her biri bir GPT Action/eklentinin istediği bir parametreyi temsil ediyor. Alanlar: `index`, `plugin_id_filenames`, `name`, `description`, `main_data_type` (25 kategori), `data_type` (145 kategori).

`notebooks/` altındaki analiz çalışmaları bu dosyayı kullanıyor; RAG backend'i de aynı dosyayı burada referans alacak.
