# Minimal dependencies for running the test suite (python -m unittest discover -s tests).
# Deliberately excludes sentence-transformers/torch and spaCy: the tests never load them.
fastapi==0.141.1
python-dotenv==1.2.3
anthropic==1.6.0
faiss-cpu==1.15.0
numpy==2.3.5
pandas==2.2.3
scipy==1.17.0
scikit-learn==1.8.0
